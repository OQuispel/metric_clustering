This folder contains the code used to run the metric comparisons in parallel using the map comparison kit.
Function.py contains various functions used to generate the necessary files and call on the MCk through command prompt
The different notebooks calculate their respective metric and store it to disk.
